#!/bin/sh

if [ "$#" -ne 1 ]; then
	echo "Usage: go <GoSourceFile.go>"
	return 1
fi

input_file="$1"
output_file="${input_file%.go}.out"

if [ ! -f "$input_file" ]; then
	echo "Error: '$input_file' not found!"
	return 1
fi

go build -o "$output_file" "$input_file"
